# generated by datamodel-codegen:
#   filename:  question_request.v1.schema.json

from __future__ import annotations

from typing import Literal

from pydantic import BaseModel, ConfigDict, Field, confloat, conint, constr


class QuestionRequestV1(BaseModel):
    """
    A single natural-language question submitted to the agent runtime, together with the database it is asked against and the optional per-run caps that bound it. Validated at the HTTP boundary before any model call is made.
    """

    model_config = ConfigDict(
        extra='forbid',
    )
    schema_: Literal['question_request.v1'] = Field(
        ..., alias='schema', description='Contract discriminator, always question_request.v1'
    )
    question: constr(min_length=1, max_length=2000) = Field(
        ..., description="The user's natural-language question"
    )
    database: constr(pattern=r'^[a-z0-9][a-z0-9_-]*$', min_length=1, max_length=128) = (
        Field(
            ...,
            description='Identifier of the database to query. Must name a database the runtime has a read-only connection configured for; it is never interpolated into SQL.',
        )
    )
    evidence: constr(max_length=4000) | None = Field(
        '',
        description='External knowledge accompanying the question. BIRD supplies this per item; the live demo leaves it empty.',
    )
    max_repair_depth: conint(ge=0, le=10) | None = Field(
        3,
        description="Cap on repair iterations for this run. Absent means use the server default; a value above the server's configured ceiling is clamped, never honoured.",
    )
    budget_usd: confloat(le=1.0, gt=0.0) | None = Field(
        0.05,
        description='Dollar cap for this run. Clamped to the server ceiling. Hitting it ends the run with a recorded budget_exhausted outcome, which is not an error.',
    )
    wall_clock_ms: conint(ge=1000, le=300000) | None = Field(
        30000,
        description='Wall-clock cap for this run in milliseconds. Clamped to the server ceiling.',
    )
